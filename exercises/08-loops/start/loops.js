// -------------------------------
// Loops
// -------------------------------

// -----------------------------------------------------------
//
// To learn loops we need to build them over and over and
// over and over and over and over
//
// and over. :P
// Read the instructions above the loop and produce the
// desired output.
//
// -----------------------------------------------------------

// Create a loop that builds a string on dash numbers
// Expected result should be '-1-2-3-4-5-6-7-8-9'
let numOutput = "";
for () {
  numOutput += `-${i}`;
}
console.log(numOutput);


// Loop through each key in the object below and display
// the key along with the corresponding value
// Expected result should be 'model --> I-280'
const myCar = {
  model: "I-280",
  make: "Izuzu",
  year: 2011,
  color: "Black",
  transmission: "Standard",
  numberOfWheels: 4
};
for (let __ in ___ ) {
  console.log(`${} --> ${}`);
}

// Loop through this array with a for of loop and
// log each value out to the console.
const characters = ["Fry", "Bender", "Leela", "Professor", "Hermies"];
for (let __ of ___) {
  console.log();
}
